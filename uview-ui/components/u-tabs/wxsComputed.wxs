function lineStyle(data) {
	return {
		transform: `translateX(${uni.$u.addUnit(data.lineOffsetLeft)})`,
		// height: data.lineHeight !== -1 ? utils.addUnit(data.lineHeight) : null,
		
	}
}

module.exports = {
	lineStyle: lineStyle
}
